server:
  #服务端口号
  port: 8861

spring:
  application:
    #服务名称
    name: service-ribbon

eureka:
  client:
    serviceUrl:
      #当前服务需要注册的注册中心服务地址（此处注册中心服务为集群）
      defaultZone: http://localhost:8881/eureka/,http://localhost:8882/eureka/